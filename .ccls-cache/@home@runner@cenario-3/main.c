#include <stdio.h>
struct list{
int numero;
 
};
int main(void) {
  void list(numero){
    adiciona(6);
    printf("adiciona o numero");
  };
    void imprime(listvazia);{
       imprime(0);
      printf("imprime uma fazialista fazia",list);
  };
    void remove(list){
      list(6);
     printf("lista fazia\n",remove);
      
    };

  return 0;
}